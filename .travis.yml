language: java
jdk:
  - oraclejdk8
before_install:
 - chmod +x gradlew
 - chmod +x CrearJars.sh
install:
./gradlew assemble
./CrearJars.sh
./gradlew check
after_failure:
 - cat build/reports/*/*.xml
cache:
 directories:
  - $HOME/.m2
  - $HOME/.gradle/wrapper
  - $HOME/.gradle/caches
notifications:
  slack: tecnicas7510-01-2016:WanMrALYQPyPe5sUnIKb8HFY
